# vim: filetype=muttrc

yabai -m signal --add event=dock_did_restart action="sudo yabai --load-sa"
sudo yabai --load-sa

# add an on_enter command to the default mode
# yabai -m config active_window_border_color 0xff775759
# yabai -m window --toggle border


yabai -m config \
    debug_output on \
    layout bsp \
    top_padding    10 \
    bottom_padding 10 \
    left_padding   10 \
    right_padding  10 \
    window_gap     10 \
    window_shadow off \
    auto_balance off \
    split_ratio 0.5


yabai -m config \
    window_opacity on \
    active_window_opacity 0.8 \
    normal_window_opacity 0.8
 
    # window_border on \
    # window_border_width 3 \

# rules

yabai -m rule --add app="^Todoist$" title="^Todoist$" manage=off sticky="true" opacity=1.0 
yabai -m rule --add app="^System Settings$" manage=off sticky="true" opacity=1.0
yabai -m rule --add app="^Dictionary$" manage=off opacity=1.0 grid=6:4:1:1:2:4
yabai -m rule --add app="^WezTerm$" title="^lf$" manage=off opacity=1.0 grid=6:4:1:1:2:4
yabai -m rule --add app="^Carbon Copy Cloner$" manage=off 
yabai -m rule --add app="^Raycast$" title="Raycast Settings" manage=off 

